SELECT NAME
FROM ANIMAL_INS
ORDER BY DATETIME ASC
LIMIT 1 # 가장 위에 레코드 추출
# LIMIT 0,3 -> 위에서 부터 0 ~ 3까지 추출




/* Oracle
SELECT NAME
FROM (SELECT *
     FROM ANIMAL_INS
     ORDER BY DATETIME)
WHERE ROWNUM < 2
*/
